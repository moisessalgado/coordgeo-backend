[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = test_*.py *_test.py tests.py
addopts = -ra

testpaths =
    api
    accounts
    organizations
    projects
    data
    permissions
    core
    tests_pytest
